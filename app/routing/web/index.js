const auth = ''
const products = require('./routes/products-route')
const categories = require('./routes/categories-route')
const carousel = require('./routes/carousel-route')

const Routes = [products, categories, carousel]

module.exports = Routes
